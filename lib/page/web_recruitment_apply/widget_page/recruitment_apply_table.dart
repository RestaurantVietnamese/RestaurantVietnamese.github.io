import 'package:flutter/material.dart';

class RecruitmentApplyTable extends StatelessWidget {
  const RecruitmentApplyTable({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('RecruitmentApplyTable'),
      ),
    );
  }
}